Посмотри, нет ли в memory MCP информации об этой статье: https://www.shuttle.dev/blog/2025/10/29/stream-http-mcp
Если есть - используй, если нет - просмотри статью и запомни под именем "Shuttle-dev-stream-mcp" (или как там организована память?) также сделай нужные relations.


Изучи как сделать следующее:
  - Вводим два режима работы для этого MCP сервера: stdio и stream.
  - По умолчанию работает в stdio без логгинга в консоль. -l и --log включают логгинг только в файл.
  - если указан флаг -s или --stream и какие там нужны ещё аргументы - переключается в стриминговый режим с нормальным логгингом в консоль. -l и --log работают так же: если просто указаны то логаем в `project-name`.log. Если указано -l name.log - логаем в это имя.
  - должны поддерживаться флаги -h, --help, /? для вывода хелпа.

Используй sub-agents, filesystem и memory mcp, ищи в интернете и context7 если надо.
Работай параллельно.
Оцени идею, спроси вопросы, сделай план реализации.